Before marking tasks done:
- Lint passes (npm run lint at root, and per workspace).
- Type-check passes (npm run check-types at root).
- Build passes (npm run build at root).
- API: run dev, verify healthz and key endpoints.
- Web: load dashboard at /, verify Projects, Tenants, Tokens, Uploads, Usage pages render and interact with API.
- Update README/POST-INIT when structure changes.
